Always reply in Simplified Chinese unless I explicitly ask for another language.

When an image-generation task should use the locally opened Chrome ChatGPT web UI, prefer the `chatgpt-web-image` skill and the scripts under `D:\codeX\scripts`.

Do not use local ChatGPT web text generation by default. Only call `chatgpt-text.js` when I explicitly ask for text to be generated through the local ChatGPT web UI or explicitly mention `chatgpt-text.js`.
